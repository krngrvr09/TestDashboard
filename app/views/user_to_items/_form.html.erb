<%= form_for(@user_to_item) do |f| %>
  <% if @user_to_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_to_item.errors.count, "error") %> prohibited this user_to_item from being saved:</h2>

      <ul>
      <% @user_to_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :item_id %><br>
    <%= f.number_field :item_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
